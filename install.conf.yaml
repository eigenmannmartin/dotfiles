- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: "vim/"
    ~/.zshrc: 'zsh/zshrc'
    ~/.tmux.conf: 'tmux.conf'

- shell:
  - [git submodule update --init --recursive, Installing submodules]
